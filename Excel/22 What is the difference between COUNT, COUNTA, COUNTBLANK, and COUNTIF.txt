Q. What is the difference between COUNT, COUNTA, COUNTBLANK, and COUNTIF?


- COUNT(range) – Counts numeric values.
  COUNTA(range) – Counts non-empty cells (numbers + text).
  COUNTBLANK(range) – Counts empty cells.
  COUNTIF(range, criteria) – Counts cells that satisfy a condition.